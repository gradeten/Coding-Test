#include <bits/stdc++.h>
using namespace std;

int main (void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int a;
    cin >> a;
    
    cout << fixed << setprecision(10) << 100.0/a << '\n';
    cout << fixed << setprecision(10) << 100.0/(100-a) << '\n';
}